# The Drayker documentation theme — one layout shared by every *.drayker.org site.
# Source: https://github.com/draykerdk/drayker-theme
remote_theme: draykerdk/drayker-theme

# Set explicitly: GitHub Pages derives site.url without TLS when the certificate is
# terminated upstream, which leaves absolute URLs in meta tags on http. Everything the
# sitemap and the social cards emit has to be the canonical https address.
url: "https://dknowledge.drayker.org"

plugins:
  - jekyll-remote-theme
  - jekyll-optional-front-matter
  - jekyll-sitemap

title: "Dknowledge"
description: "Drayker's public knowledge layer — current orientation, papers, architecture, evidence and historical context connected to their sources."

dk_kicker: "Knowledge base"
dk_subdomain: "dknowledge.drayker.org"
dk_repo: "https://github.com/draykerdk/dknowledge"

defaults:
  - scope:
      path: ""
    values:
      layout: default

dk_sphere: "star"  # the lit archive everything else is checked against

# The official mark, recoloured. Geometry is fixed for every project
# (DRAYKER-MARK.md §2.5); only the globe carries the scope.
dk_accent: "#F2E36B"  # the archive: lit parchment, older and warmer than DFM gold
